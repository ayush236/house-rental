<%- include('../partial/boiler') %>
<body class="min-h-screen bg-gradient-to-r from-blue-100 to-purple-200 flex flex-col">
  <!-- Navbar -->
  <%- include('../partial/nav') %>

  <!-- Hero Section -->
  <main class="max-w-4xl mx-auto mt-16 p-10 bg-white/80 backdrop-blur-lg rounded-3xl shadow-2xl text-center">
    <h1 class="text-4xl font-extrabold text-purple-800 mb-4 animate-pulse">
      Your Hosted Properties
    </h1>
    <p class="text-lg text-gray-600 mb-6">
      Manage your listings with ease. Edit, update, or delete any property at a click.
    </p>
  </main>

  <!-- Cards Section -->
  <section class="max-w-7xl mx-auto grid gap-8 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 mt-12 p-6">
    <% RegisterHome.forEach(house => { %>
      <div class="bg-white/90 backdrop-blur-md rounded-3xl shadow-lg overflow-hidden hover:shadow-2xl transform hover:-translate-y-2 transition duration-300">
        <!-- Card Image -->
        <img
          class="w-full h-52 object-cover rounded-t-3xl"
          src="<%= house.image || '/image/image.webp' %>"
          alt="<%= house.name %>"
        >

        <div class="p-5">
          <!-- Name -->
          <h2 class="text-xl font-semibold text-gray-800 mb-1 truncate">
            <%= house.name %>
          </h2>

          <!-- Address -->
          <p class="text-sm text-gray-500 mb-3 truncate">
            <%= house.address %>
          </p>

          <!-- Rating -->
          <div class="flex items-center mb-3">
            <div class="flex text-yellow-400">
              <% for (let i = 0; i < house.rating; i++) { %>
                <svg class="w-5 h-5 fill-current" viewBox="0 0 20 20">
                  <path d="M10 15l-5.878 3.09L5.24 12.545 1 8.91l6.06-.882L10 2.5l2.94 5.528 6.06.882-4.24 3.635 1.118 5.545z"/>
                </svg>
              <% } %>
            </div>
            <span class="ml-2 text-sm text-gray-600 font-medium">
              <%= house.rating %>/5
            </span>
          </div>

          <!-- Cost -->
          <p class="text-lg font-bold text-purple-700 mb-4">
            $<%= house.cost %> / night
          </p>

          <!-- Buttons -->
          <div class="flex gap-3">
            <a href="/host/editing-home/<%= house._id %>?editing=true" 
               class="flex-1 bg-gradient-to-r from-green-400 to-green-600 hover:from-green-500 hover:to-green-700 text-white py-2 rounded-xl font-semibold shadow-md transition transform hover:scale-105 text-center">
              Edit
            </a>
            <form action="/host/delete-home/<%= house._id %>" method="POST" class="flex-1">
              <button type="submit" 
                      class="w-full bg-gradient-to-r from-red-400 to-red-600 hover:from-red-500 hover:to-red-700 text-white py-2 rounded-xl font-semibold shadow-md transition transform hover:scale-105">
                Delete
              </button>
            </form>
          </div>
        </div>
      </div>
    <% }) %>
  </section>
</body>
</html>
